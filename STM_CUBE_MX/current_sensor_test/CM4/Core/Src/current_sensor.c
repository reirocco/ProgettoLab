#ifndef MY_HEADER_H
# define MY_HEADER_H

#define VREF
#define READING_ADC_TIME 0.2 // MILLISECONDI  utilizzati dal nucleo PER effettuare UNA RILEVAZIONE DEL ADC
/*

// puntatore alla funzione
function current_sensor_calibration(){
	uint16_t CountValue;
	uint16_t somma = 0;
	uint16_t media;
	HAL_ADC_Start(&hadc1);
	HAL_ADC_PollForConversion(&hadc1,timeout);
	CountValue = HAL_ADC_GetValue(&hadc1);


}
*/

#endif
